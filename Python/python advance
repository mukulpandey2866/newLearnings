# List and Tuples

stores set of values...like a list , could be any data type..float string integer etc.
student=["Karan", 86, "Delhi"] #eg.
print(type(student ))
print(student)
print(student[1])   #access with index.
print(len(student))  #get the no of elements in the list.

#some other use cases.
print(len(student))

#slicing: list_name[first_index:second_index] use with p-rint statementfor display
#ending index not included
print(student[1:])
#negative index is also valid
# -5-4-3-2-1 like this
#new comment
#added it here on the spot

#list operations....................
list=[2,1,3]
list.append(4) #adds one element at the end [2, 1, 3, 4]
list.sort() #sorts in ascending order [1, 2, 3]
list.sort( reverse= True) #sorts in descending order [3,2, 1]
list.reverse() #reverses list [3, 1, 2]
list.insert(idx,el) #insert an element at a particular index
list.remove(1) //reoves element
list.pop(idx)  // removes ele at index
list ou telements modified
load more


#--------------TUPLES------------
immutable sequence of values;
tup=(1,2,3,4,5,6)
out to it
awefawfe
asdcwefwef
